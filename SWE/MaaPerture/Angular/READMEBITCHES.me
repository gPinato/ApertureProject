Per far funzionare la cosa:
npm install dovrebbe installare tutti i moduli

grunt serve : avvia il server web per testare l'app
Ctrl+C per fermarlo

grunt test : esegue i test e genera il file di copertura (per ora non funziona)
