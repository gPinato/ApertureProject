/** * File: JSonComposer.js * Module: maap_server::ModelServer::DataManager * Author: Alberto Garbui * Created: 04/05/14 * Version: 0.1 * Description: JSON composer... * Modification History: ============================================== * Version | Changes ============================================== * 0.1 File creation ============================================== */'use strict';exports.createCollectionsList = function(collectionsList) {			//carico la lista di collections su data		var data = [];	data.push('cars');	data.push('models');	data.push('playBoySouthAfrica');	data.push('bennetsRulez');	data.push('testCollection');		var json =  {};	json.data = data;			var jsonString = JSON.stringify(json);	console.log('composer collectionslist:');	console.log(jsonString);		return jsonString;}exports.createCollection = function(labels,data,config) {	//il json composer dovrebbe creare una stringa json da inviare	//al client, vedi i files json presenti in maap_project/app/	//per avere un'idea della formattazione		//questo è un'esempio statico di cosa dovrebbe fare il JSON composer:		//andrà cambiato utilizzando i dati in ingresso alla funzione...	console.log('composerdata:' + data);	//creo l'array delle etichette	var labels = [];	labels.push('Id!?');	labels.push('email YEAH');	labels.push('password YEALbel');		//creo l'array di data	var dataArray = [];	for(var i=0; i<data.length; i++) {		dataArray.push({			"ID" : data[i]._id,			"data" : {				"_id" : data[i]._id,				"email": data[i].email,				"password": data[i].password			}		});		}		console.log("data array:");	console.log(dataArray);	//creo il json principale: un array di labels, data e config	//come da specifiche prese con jack	var json = [];	json.push(labels);	json.push(dataArray);		var jsonString = JSON.stringify(json);	console.log('composer:');	console.log(jsonString);		return jsonString;};exports.createDocument = function(labels,data,config) {	//il json composer dovrebbe creare una stringa json da inviare	//al client, vedi i files json presenti in maap_project/app/	//per avere un'idea della formattazione		//questo è un'esempio statico di cosa dovrebbe fare il JSON composer:		//andrà cambiato utilizzando i dati in ingresso alla funzione...	console.log('composerdata:' + data);	//creo l'array delle etichette	var labels = [];	labels.push('Id!?');	labels.push('email YEAH');	labels.push('password YEALbel');		//creo l'array di data	var dataArray = {};	dataArray._id= data._id;	dataArray.email= data.email;	dataArray.password= data.password;		console.log("data array:");	console.log(dataArray);	//creo il json principale: un array di labels, data e config	//come da specifiche prese con jack	var json = {};//,dataArray);	json.label=labels;	json.data=dataArray;	var jsonString = JSON.stringify(json);	console.log('composer:');	console.log(jsonString);		return jsonString;};